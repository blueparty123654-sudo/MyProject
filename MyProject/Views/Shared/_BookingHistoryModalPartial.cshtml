<div class="modal fade" id="bookingHistoryModal" tabindex="-1" aria-labelledby="bookingHistoryModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-xl modal-dialog-centered modal-dialog-scrollable">
        @* ทำให้ใหญ่ขึ้นและ Scroll ได้ *@
        <div class="modal-content">
            <div class="modal-header text-white">
                <h5 class="modal-title" id="bookingHistoryModalLabel"><i class="fas fa-history me-1"></i> ประวัติการจองของคุณ</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                @* -- ส่วนแสดงรายการจอง (จะถูกเติมด้วย JS) -- *@
                <div id="bookingHistoryListContainer">
                    <p class="text-center text-muted">กำลังโหลดข้อมูล...</p>
                </div>
                @* -- จบส่วนแสดงรายการ -- *@

                @* แสดงข้อความ Error ถ้าโหลดไม่สำเร็จ *@
                <div id="bookingHistoryErrorAlert" class="alert alert-danger mt-3" role="alert" style="display: none;"></div>
            </div>
        </div>
    </div>
</div>